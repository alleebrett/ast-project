 import React from 'react'; var GameBoardCell = React.createClass({  getInitialState: function() {    return {cellState: false};  },  handleClick: function(event) {    this.setState({cellState: !this.state.cellState});  },  render: function() {    var myCellState = this.state.cellState ? 'On' : 'Off';    return (      <p onClick={this.handleClick} style={{color: 'red', textAlign: 'center', backgroundColor: 'blue', width: 50, padding: 50}}>       {myCellState}      </p>    );  }});setInterval(function() {  React.render(<GameBoardCell />, document.body);}, 1000);